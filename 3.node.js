// commonJS , every fike is module (by default)
// modules - Encapsulated code (only share minimum)
const secret = 'SUPER SECRET'
const ansh = 'ansh'
const jishan = 'jishan'

const sayHi = (name) => {

    console.log('hello there ' + name)
}

sayHi('don')
sayHi(ansh)
sayHi(jishan)